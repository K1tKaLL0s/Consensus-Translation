# 隐私与许可

默认配置优先本地处理，不启用遥测。任何未来遥测功能都必须在隐私说明中披露，并提供明确开关。

远端 provider 只在用户配置并确认后调用。工作台预检会列出数据范围、预算和需要确认的远端调用。

API key 保存在本机凭据存储中，界面不会显示明文密钥。

商业安全 profile 不打包 `facebook/nllb-200-distilled-600M`。NLLB 属于 CC-BY-NC-4.0 的非商业模型，只能在 research profile 和用户自行接受许可后使用。
